# Requirements for Windows Portable Package
# CPU-only version to minimize package size
#
# Usage:
#   python -m pip install -r requirements_windows_portable.txt
#
# Size estimates:
#   - PyTorch CPU: ~150 MB (vs 3 GB for CUDA)
#   - Total dependencies: ~350 MB

# PyTorch CPU-only (download separately to avoid Linux-only deps)
--find-links https://download.pytorch.org/whl/cpu/torch_stable.html
torch==2.4.1
torchvision==0.19.1

# Web interface
gradio>=6.0.0,<7.0.0

# Image processing
opencv-python-headless>=4.8.0
Pillow>=11.0.0

# Scientific computing
numpy>=2.1.0,<3.0.0
scipy>=1.14.0
scikit-learn>=1.5.0

# Visualization
matplotlib>=3.10.0

# Data manipulation
pandas>=2.2.0

# PDF export
reportlab>=4.2.0

# Additional utilities (if needed by the GUI)
tqdm>=4.66.0
