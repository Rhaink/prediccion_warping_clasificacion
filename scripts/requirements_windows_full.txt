# Complete Windows portable dependency list
# Python: 3.12.8, Platform: Windows 10/11 x64
# Last updated: 2026-01-20
#
# This file is the SINGLE SOURCE OF TRUTH for all dependencies in the Windows portable package.
# It includes dependencies that are lost when using --no-deps with torch, as well as
# Windows-specific packages whose environment markers don't evaluate correctly when
# downloading from Linux.

# =============================================================================
# PyTorch CPU + Dependencies Lost by --no-deps
# =============================================================================
# Note: torch and torchvision are downloaded separately with --no-deps to avoid
# pytorch-triton-rocm (Linux-only, 500+ MB). The following are CRITICAL dependencies
# that --no-deps removes but are required for torch to function:

sympy>=1.12              # torch.fx symbolic computation (CRITICAL)
mpmath>=1.1.0,<1.4       # sympy mathematical backend (CRITICAL)
networkx>=2.8            # torch graph operations (CRITICAL)
filelock>=3.0            # torch resource locking
fsspec>=2022.1.0         # torch filesystem operations
setuptools>=60.0         # torch utilities
typing-extensions>=4.8   # type hints (required by torch + gradio)
jinja2>=3.0              # code generation (torch.jit + gradio)

# =============================================================================
# Web Interface (Gradio + Full Dependency Stack)
# =============================================================================
gradio>=6.0.0,<7.0.0

# Gradio CLI framework (via typer)
typer>=0.9.0
click>=8.0               # typer dependency + CLI framework
rich>=10.0               # terminal formatting
shellingham>=1.3.0       # shell detection

# Gradio HTTP server (via uvicorn + starlette)
uvicorn>=0.27.0
starlette>=0.35.0
anyio>=3.0               # async I/O primitives
sniffio>=1.0             # async library detection
h11>=0.12                # HTTP/1.1 protocol
httpcore>=1.0            # HTTP primitives
httpx>=0.24.0            # HTTP client
websockets>=10.0         # WebSocket support (CRITICAL for Gradio)

# Gradio utilities
python-multipart>=0.0.5  # multipart form data
orjson>=3.0              # fast JSON serialization
semantic-version>=2.10   # version parsing
watchfiles>=0.13         # file monitoring (Gradio auto-reload)

# SSL/Security
certifi>=2022.0          # SSL certificates
cryptography>=41.0       # security primitives

# =============================================================================
# Image Processing
# =============================================================================
opencv-python-headless>=4.8.0
Pillow>=11.0.0

# =============================================================================
# Scientific Computing
# =============================================================================
numpy>=2.1.0,<3.0.0
scipy>=1.14.0
scikit-learn>=1.5.0
pandas>=2.2.0

# =============================================================================
# Visualization
# =============================================================================
matplotlib>=3.10.0

# =============================================================================
# PDF Export
# =============================================================================
reportlab>=4.2.0

# =============================================================================
# Utilities
# =============================================================================
tqdm>=4.66.0
pyyaml>=6.0              # YAML configuration

# =============================================================================
# Windows-Specific (environment marker workaround)
# =============================================================================
# These packages have platform_system == 'Windows' markers that pip download
# from Linux doesn't evaluate correctly. We explicitly include them here.

colorama>=0.4.0          # terminal colors (click, tqdm on Windows)
python-dateutil>=2.8     # date utilities
six>=1.16                # Python 2/3 compatibility
pytz>=2023.3             # timezone handling

# =============================================================================
# Additional Gradio Dependencies
# =============================================================================
# These are indirect dependencies that may not be pulled correctly

annotated-types>=0.6.0   # pydantic support
pydantic>=2.5.0          # data validation
pydantic-core>=2.14.0    # pydantic core
fastapi>=0.109.0         # Gradio backend framework
ffmpy>=0.3.0             # video processing (Gradio media)
huggingface-hub>=0.20.0  # model hub integration
aiofiles>=23.0           # async file operations
altair>=5.0              # declarative visualization
markdown-it-py>=3.0      # markdown rendering
mdurl>=0.1.0             # markdown URL utilities
pygments>=2.16.0         # syntax highlighting
exceptiongroup>=1.1.0    # exception handling (Python 3.10 backport)

# =============================================================================
# EXCLUDED (Platform-specific, must avoid)
# =============================================================================
# pytorch-triton-rocm - Linux x86_64 only (500+ MB, causes build to fail)
# pywin32 - Windows-only but installs via system, not pip
